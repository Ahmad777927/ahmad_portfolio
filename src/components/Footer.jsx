import React from 'react'

export default function Footer() {
  return (
    <footer className="px-8 py-14 text-center">
      <div className="mb-3.5 flex justify-center font-mono text-sm font-semibold tracking-[0.02em]">
        ahmad<span className="text-[var(--amber)]">.dev</span>
      </div>
      {/* <p className="font-mono text-[13px] text-[var(--text-dim)]">
        Built for <span className="text-[var(--amber)]">Ahmad</span> — swap placeholder thumbnails once project imagery is ready.
      </p> */}
    </footer>
  )
}
